spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jdbc_demo
    username: postgres
    password: Postgre8951
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081